import React from "react";
import { Typography } from "@mui/material";

const Typo = () => {
  return (
    <div>
      <Typography variant="h3" mt={2}>
        Text written inside the Typography
      </Typography>
    </div>
  );
};

export default Typo;
